# Dice Roller
Simulates rolling the number of  dices depending on user's input. Displays a random number for each roll using random module and displays the output of dices rolled using ascii art.

## What I Learned:
- Random number generation
- Loops and simple conditions
- Use of dictionaries and it's different methods(get.items(),get.values() etc